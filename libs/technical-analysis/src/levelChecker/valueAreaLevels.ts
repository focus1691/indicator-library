export enum DAILY_OPEN_STATE {
  OUTSIDE = 'Outside',
  OUTSIDE_ABOVE = 'Outside Above',
  OUTSIDE_BELOW = 'Outside Below',
  INSIDE = 'Inside'
}

export enum INITIAL_BALANCE_STATE {
  INSIDE = 'Inside',
  OUTSIDE = 'Outside',
  BELOW = 'Below',
  ABOVE = 'Above'
}
